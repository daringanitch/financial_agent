apiVersion: v1
kind: Service
metadata:
  name: financial-agent-web
  namespace: financial-agent
  labels:
    app: financial-agent-web
spec:
  type: NodePort
  selector:
    app: financial-agent
  ports:
  - port: 8501
    targetPort: 8501
    nodePort: 30001
    protocol: TCP
    name: streamlit-web